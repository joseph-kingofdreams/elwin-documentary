---
export interface Props {
    name: string;
    img?: string;
}

const { name, img } = Astro.props;
---

<div class="flex flex-col gap-2 flex-1">
    <div class="bg-orange-500 min-w-52 aspect-[27/40] rounded-md"></div>
    <p class="text-white text-semibold">{name}</p>
</div>
